export const  smartardioSliderImage = Array.from({length : 8}).map( (e, i) => {
    return `/images/smartcardioS${i+1}.webp`
} )

export const  recordersSliders = Array.from({length : 3}).map( (e, i) => {
    return `/images/example${i+1}.webp`
} )


